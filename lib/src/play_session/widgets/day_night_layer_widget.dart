import 'package:flutter/material.dart';

class DayNightLayerWidget extends StatelessWidget {
  const DayNightLayerWidget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Color(0xFFBFDAB0));
  }
}
